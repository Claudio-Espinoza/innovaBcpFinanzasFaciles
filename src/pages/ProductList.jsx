import React from 'react';
import AppBar from '../components/AppBar.jsx';

const ProductList = () => {
  return (
    <div>
      <AppBar />
      {/* Contenido de la página de lista de productos */}
    </div>
  );
};

export default ProductList;
